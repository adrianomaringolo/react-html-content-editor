import { version } from "@sympro/react-html-content-editor";

function App() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>React HTML Content Editor Demo</h1>
      <p>Library version: {version}</p>
      <p>
        Demo app is ready. Components will be added as they are implemented.
      </p>
    </div>
  );
}

export default App;
